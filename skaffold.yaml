apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/rheinwein/example-voting-app:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/rheinwein/example-voting-app:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: example-voting-app
        chartPath: charts/example-voting-app
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/rheinwein/example-voting-app"
          image.tag: "{{.DIGEST_HEX}}"
